# Classe che contiene gli ostacoli, i confini e funzioni di utilità geometriche

from shapely.geometry import Polygon, box
import matplotlib.pyplot as plt

class Environment:
    """Placeholder per la gestione dell'ambiente.
    In futuro qui verranno implementati:
      - Ostacoli (tramite shapely)
      - Limiti del mondo
      - Collision checking
    Per ora serve solo come struttura vuota."""

    def __init__(self):
        pass

    def plot(self, ax):
        """Placeholder per il disegno dell’ambiente (attualmente vuoto)."""
        pass